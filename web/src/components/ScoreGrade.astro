---
import { gradeColors } from "../lib/constants";

interface Props {
  grade: string;
  score?: number;
  size?: "sm" | "md" | "lg";
}

const { grade, score, size = "md" } = Astro.props;

const sizeClasses: Record<string, string> = {
  sm: "w-8 h-8 text-sm",
  md: "w-12 h-12 text-xl",
  lg: "w-16 h-16 text-3xl",
};

const colorClass = gradeColors[grade]?.css ?? gradeColors.C.css;
const sizeClass = sizeClasses[size];
---

<div class="flex items-center gap-2">
  <div class={`${sizeClass} ${colorClass} rounded-lg border-2 flex items-center justify-center font-bold`}>
    {grade}
  </div>
  {score !== undefined && (
    <span class="text-sm text-gray-400">{score}/100</span>
  )}
</div>
